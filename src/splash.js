$(document).ready(function() {
	/*setInterval(function() {
		var text = $("#loading-text").html();
		if(text == "Loading...") {
			text = "Loading";
		}
		else {
			text += ".";
		}

		$("#loading-text").html(text);
	}, 1000);*/
});
